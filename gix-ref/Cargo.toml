lints.workspace = true

[package]
name = "gix-ref"
version = "0.64.0"
repository = "https://github.com/GitoxideLabs/gitoxide"
license = "MIT OR Apache-2.0"
description = "A crate to handle git references"
authors = ["Sebastian Thiel <sebastian.thiel@icloud.com>"]
edition = "2024"
include = ["/src/**/*", "/LICENSE-*"]
rust-version = "1.85"

[lib]
doctest = false
test = true

[features]
default = ["reftable"]
## Enable support for the SHA-1 hash by enabling the respective feature in the `gix-hash` crate.
sha1 = ["gix-hash/sha1", "gix-reftable?/sha1"]
## Enable support for the SHA-256 hash by enabling the respective feature in the `gix-hash` crate.
sha256 = ["gix-hash/sha256", "gix-reftable?/sha256"]
## Enable support for reading and writing reftable-backed reference stores using vendored C.
reftable = ["dep:gix-reftable"]
## Data structures implement `serde::Serialize` and `serde::Deserialize`.
serde = ["dep:serde", "gix-hash/serde", "gix-actor/serde", "gix-object/serde"]
## Enable support for thread-safety.
parallel = ["gix-features/parallel"]

[dependencies]
gix-features = { version = "^0.48.1", path = "../gix-features", features = ["walkdir"] }
gix-fs = { version = "^0.21.2", path = "../gix-fs" }
gix-path = { version = "^0.12.1", path = "../gix-path" }
gix-hash = { version = "^0.25.1", path = "../gix-hash" }
gix-object = { version = "^0.61.0", path = "../gix-object" }
gix-utils = { version = "^0.3.3", path = "../gix-utils" }
gix-validate = { version = "^0.11.2", path = "../gix-validate" }
gix-actor = { version = "^0.41.1", path = "../gix-actor" }
gix-lock = { version = "^23.0.0", path = "../gix-lock" }
gix-tempfile = { version = "^23.0.0", default-features = false, path = "../gix-tempfile" }
gix-reftable = { version = "^0.1.0", path = "../gix-reftable", optional = true, default-features = false }

thiserror = "2.0.18"
serde = { version = "1.0.114", optional = true, default-features = false, features = ["derive"] }

# packed refs
memmap2 = "0.9.10"

document-features = { version = "0.2.1", optional = true }

[dev-dependencies]
gix-testtools = { path = "../tests/tools", default-features = false }
gix-date = { path = "../gix-date" }
gix-hash = { path = "../gix-hash", features = ["sha1", "sha256"] }
gix-discover = { path = "../gix-discover" }
gix-odb = { path = "../gix-odb" }
insta = "1.46.3"
tempfile = "3.26.0"

[package.metadata.docs.rs]
features = ["sha1", "document-features", "serde"]
